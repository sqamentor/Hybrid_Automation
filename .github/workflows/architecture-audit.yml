name: 🏗️ Architecture Audit

# Auto-trigger on ANY change to Python files - enforces compliance dynamically
on:
  pull_request:
    branches: [main, develop]
    types: [opened, synchronize, reopened]
    paths:
      - '**.py'
      - 'tests/**'
      - 'pages/**'
      - 'framework/**'
      - 'utils/**'
  push:
    branches: [main, develop, feature/**, bugfix/**]
    paths:
      - '**.py'
      - 'tests/**'
      - 'pages/**'
      - 'framework/**'
      - 'utils/**'
  workflow_dispatch:

# Ensure only one audit runs at a time per PR
concurrency:
  group: audit-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # ═══════════════════════════════════════════════════════════════════════════
  # INDEPENDENT RULE CATEGORY CHECKS
  # Each category runs as independent status check for PR visibility
  # ═══════════════════════════════════════════════════════════════════════════
  
  engine-mix:
    name: 'audit/engine-mix'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          pip install pyyaml
      
      - name: Run engine-mix check
        run: |
          python ci/ci_audit_runner.py --check engine-mix --artifacts-dir artifacts/engine-mix
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: engine-mix-report
          path: artifacts/engine-mix/
  
  marker-engine:
    name: 'audit/marker-engine'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: pip install pyyaml
      
      - name: Run marker-engine check
        run: |
          python ci/ci_audit_runner.py --check marker-engine --artifacts-dir artifacts/marker-engine
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: marker-engine-report
          path: artifacts/marker-engine/
  
  folder-engine:
    name: 'audit/folder-engine'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: pip install pyyaml
      
      - name: Run folder-engine check
        run: |
          python ci/ci_audit_runner.py --check folder-engine --artifacts-dir artifacts/folder-engine
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: folder-engine-report
          path: artifacts/folder-engine/
  
  pom-compliance:
    name: 'audit/pom-compliance'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: pip install pyyaml
      
      - name: Run pom-compliance check
        run: |
          python ci/ci_audit_runner.py --check pom-compliance --artifacts-dir artifacts/pom-compliance
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pom-compliance-report
          path: artifacts/pom-compliance/
  
  test-boundaries:
    name: 'audit/test-boundaries'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: pip install pyyaml
      
      - name: Run test-boundaries check
        run: |
          python ci/ci_audit_runner.py --check test-boundaries --artifacts-dir artifacts/test-boundaries
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-boundaries-report
          path: artifacts/test-boundaries/
  
  structural:
    name: 'audit/structural'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: pip install pyyaml
      
      - name: Run structural check
        run: |
          python ci/ci_audit_runner.py --check structural --artifacts-dir artifacts/structural
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: structural-report
          path: artifacts/structural/
  
  canonical-flow:
    name: 'audit/canonical-flow'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: pip install pyyaml
      
      - name: Run canonical-flow check
        run: |
          python ci/ci_audit_runner.py --check canonical-flow --artifacts-dir artifacts/canonical-flow
      
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: canonical-flow-report
          path: artifacts/canonical-flow/
  
  # ═══════════════════════════════════════════════════════════════════════════
  # COMBINED AUDIT REPORT & PR COMMENT
  # ═══════════════════════════════════════════════════════════════════════════
  
  combined-report:
    name: 'Combined Audit Report'
    runs-on: ubuntu-latest
    needs: [engine-mix, marker-engine, folder-engine, pom-compliance, test-boundaries, structural, canonical-flow]
    if: always()
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          pip install pyyaml requests
      
      - name: Generate combined report
        continue-on-error: true
        run: |
          python ci/ci_audit_runner.py --artifacts-dir artifacts/ || echo "Report generation failed"
      
      - name: Create report if missing
        if: always()
        run: |
          mkdir -p artifacts
          if [ ! -f artifacts/framework_audit_report.md ]; then
            echo "# Architecture Audit Report" > artifacts/framework_audit_report.md
            echo "" >> artifacts/framework_audit_report.md
            echo "⚠️ Report generation failed or incomplete." >> artifacts/framework_audit_report.md
            echo "Check individual job logs for details." >> artifacts/framework_audit_report.md
          fi
      
      - name: Upload combined report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: framework-audit-report
          path: artifacts/framework_audit_report.md
          if-no-files-found: warn
      
      - name: Comment on PR
        if: github.event_name == 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python ci/github_pr_commenter.py \
            --pr-number ${{ github.event.pull_request.number }} \
            --baseline ci/baseline_allowlist.yaml || true
      
      - name: Check if audit passed
        continue-on-error: true
        run: |
          if [ -f artifacts/audit_failed ]; then
            echo "❌ Architecture audit failed - see reports for details"
            exit 1
          fi
          echo "✅ Architecture audit passed"
  
  # ═══════════════════════════════════════════════════════════════════════════
  # SUMMARY COMMENT
  # ═══════════════════════════════════════════════════════════════════════════
  
  audit-summary:
    name: 'Audit Summary'
    runs-on: ubuntu-latest
    needs: [combined-report]
    if: always()
    steps:
      - uses: actions/checkout@v4
      
      - name: Download combined report
        if: github.event_name == 'push'
        continue-on-error: true
        id: download-artifact
        uses: actions/download-artifact@v4
        with:
          name: framework-audit-report
          path: artifacts/
      
      - name: Check if report exists
        id: check-report
        run: |
          if [ -f artifacts/framework_audit_report.md ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ Report file downloaded successfully"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ Report file not found - will show basic summary"
          fi
      
      - name: Summary
        run: |
          echo "## 🏗️ Architecture Audit Complete"
          echo ""
          echo "Check individual status checks above for detailed results."
          echo ""
          echo "**Download artifacts to see detailed reports.**"
      
      # Archive reports with git metadata for audit trail
      - name: Archive Report with Git Metadata
        if: github.event_name == 'push' && steps.check-report.outputs.exists == 'true'
        run: |
          mkdir -p artifacts/archive
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          COMMIT_SHA=$(git rev-parse --short HEAD)
          BRANCH=$(git rev-parse --abbrev-ref HEAD)
          
          if [ -f artifacts/framework_audit_report.md ]; then
            cp artifacts/framework_audit_report.md \
               "artifacts/archive/audit_${TIMESTAMP}_${BRANCH}_${COMMIT_SHA}.md"
            echo "✅ Report archived with git metadata"
          else
            echo "⚠️ No report file found to archive"
          fi
      
      - name: Upload Archived Reports
        if: github.event_name == 'push' && steps.check-report.outputs.exists == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: audit-archive
          path: artifacts/archive/
          retention-days: 90
          if-no-files-found: warn

